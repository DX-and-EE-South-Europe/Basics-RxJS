import { BaseNav } from '../../interfaces/interfaces';

export const navObservables: BaseNav = {
  name: 'Observables',
  isSimple: true,
  labels: ['creating observable', 'subscribing', 'executing', 'disposing']
};
